<template>
  <div
    id="delAccountModal"
    ref="delAccountModal"
    class="modal fade"
    tabindex="-1"
    aria-labelledby="delAccountModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog">
      <div class="modal-content border-0">
        <div class="modal-header bg-danger text-white">
          <h5 id="delAccountModalLabel" class="modal-title">
            <span>刪除帳戶</span>
          </h5>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="modal"
            aria-label="Close"
          ></button>
        </div>
        <div class="modal-body">
          是否刪除
          <strong class="text-danger">{{ tempUser.name }}</strong>
          帳戶(刪除後將無法恢復)。
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">
            取消
          </button>
          <button type="button" class="btn btn-danger" @click="delUser">確認刪除</button>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import { Modal } from 'bootstrap';
export default {
  props: ['tempUser', 'delUser'],
  data() {
    return {
      delModalAccount: null
    };
  },
  methods: {
    openDelModal() {
      this.delModalAccount.show();
    },
    closeDelModal() {
      this.delModalAccount.hide();
    }
  },
  mounted() {
    this.delModalAccount = new Modal(this.$refs.delAccountModal);
  }
};
</script>
